export type Profile = { id: string; email: string; full_name: string; role: 'client' | 'agency'; account_status: 'active' | 'suspended'; created_at: string }
export type AgencyRequest = { id: string; profile_id: string; business_name: string; rc_number: string; document_path: string; status: 'pending' | 'approved' | 'rejected' | 'needs_changes'; admin_note: string | null; created_at: string; reviewed_at: string | null }
export type Vehicle = { id: string; agency_id: string; brand: string; model: string; category: string; year: number | null; daily_price_cents: number; active: boolean; availability: string; photos: string[]; description: string; created_at: string }
export type ReservationStatus = 'request' | 'confirmed' | 'deposit_paid' | 'delivered' | 'completed' | 'cancelled'
export type Reservation = { id: string; reference: string; client_id: string; agency_id: string; vehicle_id: string; start_date: string; end_date: string; total_cents: number; deposit_cents: number; commission_cents: number; status: ReservationStatus; admin_note: string; created_at: string; updated_at: string }
export type Payment = { id: string; reservation_id: string; kind: 'deposit' | 'balance'; source: string; chargily_reference: string | null; amount_cents: number; commission_cents: number; refunded_cents: number; refunded_commission_cents: number; agency_transferred_cents: number; status: 'pending' | 'confirmed' | 'failed' | 'refunded'; confirmed_at: string | null; refunded_at: string | null; created_at: string; updated_at: string; admin_note: string }
export type Conversation = { id: string; client_id: string; agency_id: string; reservation_id: string | null; subject: string; status: string; created_at: string }
export type Message = { id: string; conversation_id: string; sender_id: string; body: string; created_at: string }
export type ReadReceipt = { conversation_id: string; admin_id: string; last_read_at: string }
export type AuditLog = { id: string; admin_id: string | null; admin_email: string | null; action: string; entity_type: string; entity_id: string; before_data: Record<string, unknown>; after_data: Record<string, unknown>; created_at: string }
export type AdminData = { verifications?: { client_id: string; status: 'pending' | 'verified' }[]; errors?: Record<string, string>; profiles: Profile[]; agencies: AgencyRequest[]; vehicles: Vehicle[]; reservations: Reservation[]; payments: Payment[]; conversations: Conversation[]; messages: Message[]; reads: ReadReceipt[]; audit: AuditLog[] }
